this is a hello library

how to install git
	sudo apt install git

mkdir git-tutorial
git init .

git config --global user.name "arpan.v"
git config --global user.email "arpan.V@gmail.com"

git status   #to track the file modifications

git add file-name	#to add modified files to commit
git commit -m "description of modification"	#commts the added files to commit

git diff	#show only modified files
git show	#show modification after commited git
 
git commit --amend	#
git log
